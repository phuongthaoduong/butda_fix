# 测试文件夹 / Test Directory

此文件夹包含用于测试 Being-Up-To-Date Assistant 各项功能的测试脚本。

## 📁 文件说明

| 文件 | 描述 | 用法 |
|------|------|------|
| `run_all_tests.sh` | 运行所有测试的主脚本 | `./run_all_tests.sh` |
| `test_services.sh` | 测试所有服务运行状态 | `./test_services.sh` |
| `test_frontend.sh` | 测试前端服务 | `./test_frontend.sh` |
| `test_api.py` | 测试后端API功能 | `python3 test_api.py` |

## 🚀 快速开始

### 运行所有测试
```bash
cd test
./run_all_tests.sh
```

### 单独运行测试

```bash
# 测试服务状态
./test_services.sh

# 测试前端
./test_frontend.sh

# 测试API
python3 test_api.py
```

## ✅ 测试覆盖

### 服务状态测试 (`test_services.sh`)
- 工具服务器 (Port 8000)
- 后端API (Port 8001)
- 前端服务 (Port 5173)
- API文档访问

### 前端测试 (`test_frontend.sh`)
- 主页可访问性
- HTML内容类型
- Vite开发服务器
- 静态资源
- 端口监听状态

### API测试 (`test_api.py`)
- 健康检查 `/api/health`
- API文档 `/docs`
- OpenAPI规范 `/openapi.json`
- 研究API `POST /api/research`
- 流式API `GET /api/research/stream`

## 📋 前置条件

运行测试前，请确保：

1. 所有服务已启动
   ```bash
   ./scripts/start-toolserver.sh  # 终端1
   ./scripts/start-backend.sh     # 终端2
   ./scripts/start-frontend.sh    # 终端3
   ```

2. 已安装必要依赖
   - curl
   - Python 3.x
   - requests (Python库)

## 🔧 安装Python依赖

如果没有安装 `requests` 库：
```bash
pip install requests
# 或者
pip3 install requests
```

## 📊 测试输出示例

```
╔══════════════════════════════════════════════════════════════╗
║         Being-Up-To-Date Assistant - 完整测试套件            ║
╚══════════════════════════════════════════════════════════════╝

▶️  运行服务状态测试...
   ✅ 工具服务器运行中
   ✅ 后端API运行中且健康
   ✅ 前端服务运行中
   ✅ API文档可访问

📊 测试结果: 4 通过 / 0 失败
🎉 所有测试通过！
```

