{
  "app_dir": "server",
  "build_command": "pip install custom_packages/agenthub_sdk-0.1.4-py3-none-any.whl && pip install fastapi uvicorn[standard] pydantic python-dotenv redis httpx sse-starlette",
  "start_command": "uvicorn main:app --host 0.0.0.0 --port $PORT"
}
